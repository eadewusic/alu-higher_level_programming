-- Task: Create the database hbtn_0c_0 if it does not exist

-- Check if the database already exists
-- USE hbtn_0c_0;

-- If the USE statement above does not fail, it means the database exists, so exit without doing anything
-- If the database does not exist, an error will occur, and we can then proceed to create it

-- Create the database hbtn_0c_0
CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
